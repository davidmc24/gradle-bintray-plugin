apply plugin: 'groovy'

repositories {
    maven {
        name = 'BintrayJCenter'
        url = 'http://jcenter.bintray.com'
    }
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    testCompile 'org.spockframework:spock-core:0.6-groovy-1.8'
}
